{{--
    Section label in the sidebar. Collapses to a plain divider rule when the sidebar is in
    icon-only mode (see the .sidebar-collapsed rules in the layout), so the grouping still reads
    without the text.
--}}
<p class="nav-heading px-4 pt-5 pb-1 text-[11px] font-semibold uppercase tracking-wider text-slate-500 select-none">
    {{ $slot }}
</p>
